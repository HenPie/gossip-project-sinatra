<html>
  <head>
    <title>The Gossip Project</title>
  </head>
  <body>
    <h1>PAGE POTIN</h1>
    <h2>Voici la page du potin n°<%= params["id"] %></h2>
    <p>
      Son auteur : <%= Gossip.find(params["id"])[params["id"].to_i][0] %><br/>
      Le contenu du potin : <%= Gossip.find(params["id"])[params["id"].to_i][1] %>
    </p>
    <p><a href="/">Lien pour revenir à la page des potins</a></p>
  </body>
</html>